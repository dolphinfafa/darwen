# 工作记录 2026-05-11

## 主题：V2 M2 全部完成 — 指标预计算引擎落地

承接 2026-05-09 M1 数据基础完成，正式启动并完成 M2 指标预计算的全部子任务。本次产出 M2.1（字段映射 + helpers）、M2.2（ROCE 严格口径 + 5Y/10Y 门槛）、M2.3（杠杆 + 现金质量）、M2.4（稀释 + 估值）、M2.5（调度器整合 + 全量回填），并发现 + 修补了 SEC companyfacts 字段缺口、`get_fiscal_year_end` 财年末误判等核心 bug。

---

## 一、规划

新建 `milestones/v2-plan.md` — M2-M7 完整实施规划，覆盖字段映射表、ROCE 严格口径、reason_codes 命名、AI 融合规则、API endpoints 清单、前端 6 页面拆解、回测验收。10 个 TaskCreate 任务带依赖图（M2.1 → {M2.2, M2.3, M2.4} → M2.5 → M3 → M4 → M5 → M6 → M7）。

---

## 二、M2.1 字段映射 & helpers

### 交付

| 文件 | 说明 |
|---|---|
| `backend/metrics/__init__.py` | 模块说明 |
| `backend/metrics/field_map.py` | 24 canonical 字段 → SEC us-gaap / TS-FS / AKSHARE 三向映射 + SOURCE_PREFERENCE 数据源优先级链 |
| `backend/metrics/helpers.py` | `get_fact_value` / `get_fact_value_asof` / `get_annual_periods` / `get_fiscal_year_end` |

### 关键发现

- **fact.account_code 字段语义错位**：M1 重构时把 taxonomy 名（"us-gaap" / "cn-cas"）写到了 account_code，真实 tag 在 concept 字段。helpers 已按 source_type + concept 候选回退查询适配。
- **跨 source 回退**：A 股 helpers 优先 TS-FS（10 家有数据），其余 40 家自动回退 AKSHARE；美股仅 SEC。
- **严格年报模式**：`annual_only=True` + `fiscal_year_end_month` 仅取月份匹配的 fact，A 股 fy_end_month=12，美股按 Revenues period_end 月份众数推断（Apple → 9 月）。
- **MySQL 兼容**：`.nullslast()` 替换为 `case((col.is_(None), 1), else_=0)` 表达式。

### 覆盖率（5 美股 + 5 A 股样本）

| 类型 | 核心字段 | 缺口 |
|---|---|---|
| US | revenue / operating_income / net_income / cfo / capex / cash / current_assets / current_liabilities / long_term_debt / net_ppe / total_assets / shares_outstanding 全部 5/5 | short_term_investments / short_term_debt / current_portion_long_term_debt / current_lease_liability 大量缺失 |
| CN_A | 核心字段 80-100% | short_term_investments 仅 TS-FS 部分覆盖 |

---

## 三、M2.2 ROCE 严格口径

### 交付

`backend/metrics/roce.py`：
- `RoceComponents` dataclass：含 year / period_end / ebit / operating_current_assets / operating_current_liabilities / nwc_ex_cash / net_ppe / capital_employed / roce / notes / source_fact_ids
- `compute_roce_series(db, cid, year_range)` — 批量预拉缓存 + 逐年组装
- `compute_roce_year` — 单年便捷入口
- `evaluate_quality_gate(series, threshold=0.20)` — 5Y/10Y 门槛判定，`QualityGate` 含 fail_reason

### 严格口径实现

```
ROCE_t = EBIT_t / CapitalEmployed_t
CapitalEmployed_t = (CurrentAssets - Cash - ShortTermInvestments)
                   - (CurrentLiabilities - ShortTermDebt - CurrentPortionLTD - CurrentLeaseLiability)
                   + NetPPE
```

- EBIT 优先级：US = OperatingIncomeLoss；CN_A = ebit_reported → operating_income + interest_expense → total_profit + interest_expense
- NetFixedAssets 仅取 net_ppe，剔除 goodwill / 长期股权投资 / 无形资产 / 金融资产
- short_term_investments 缺失置 0 + `EXCESS_CASH_PROXY_LOW_CONFIDENCE` 标签
- 短期借款 / 一年内到期长债 / 一年内租赁负债缺失一律置 0
- CapitalEmployed ≤ 0 → `NEGATIVE_OR_ZERO_CAPITAL_EMPLOYED`，roce=None，不自动排除

### 质量门槛逻辑修正

**首版 bug**：`evaluate_quality_gate` 取"最近 5 个 valid 年份"，Apple/Visa 等 NEGATIVE_CAP_EMP 公司用早期 valid 年补窗口"假阳通过"。

**修正后**：取"最近 5 个**完整财年**"（含 invalid），近 5 年内 ≥ 2 年 NEGATIVE_CAP_EMP 或字段缺直接走 `Q5_RECENT_NEG_CAP_OR_MISSING` 人工覆核路径。

### 算法验证（10 家样本）

| 公司 | 5Y 中位数 | pass5y | strong | 备注 |
|---|---:|---|---|---|
| Microsoft | 29.7% | ✓ | ✓ | 与常识吻合 |
| Nvidia | 39.7% | ✓ | ✓ | |
| Tesla | 34.2% | ✓ | ✗ | |
| 美的集团 | 91.8% | ✓ | ✓ | 制造业 + 大现金 |
| 泸州老窖 | 105.7% | ✓ | ✓ | 白酒行业典型 |
| 茅台 | 66.9% | ✓ | ✗ | 10Y 不足 8 年 valid |
| 京东方 | 5.2% | Q3_FAIL_MEDIAN | ✗ | 重资本面板厂 |
| 格力 | NEGATIVE_CAP_EMP | Q5 覆核 | — | 结构性负 NWC |
| 广发证券 | n/a | Q0 排除 | — | 券商资产结构不适用 |

---

## 四、M2.5 ROCE 全量回填（部分）

### 交付

`backend/metrics/compute.py`：
- `_build_fact_meta_lookup` — 批量查 fact_id → (source_type, raw_value, accepted_date)
- `_upsert_metric` — MySQL `INSERT ... ON DUPLICATE KEY UPDATE`
- `_write_lineage_rows` — 每输入字段一行 metric_lineage_log，含 source_code / raw_value / effective_date
- `persist_roce_for_company` — 单家落库 + 截面指标
- `persist_roce_bulk` — 批量调度，进度日志
- CLI: `python -m backend.metrics.compute [--company X] [--start-year 2014] [--end-year 2024]`

### 写库规约

唯一键：`(company_id, period_end, metric_name, formula_version)`
formula_version：`roce_v1`
落库的 metric_name：

年度（每年一行）：roce / ebit / capital_employed / nwc_ex_cash / operating_current_assets / operating_current_liabilities / net_ppe

截面（最新 period_end 一行）：roce_5y_median / roce_10y_median / q3_pass_5y / q4_strong_track_record，notes 字段携带 fail_reason

### 全量回填结果

```
total=598, ok=598, errors=0
year_rows=31,423
section_rows=1,988
lineage_rows=32,161
pass_5y_count=153 (25.6%)
strong_count=129 (21.6%)
```

耗时约 4 分钟，0.41 s/家。

---

## 五、SEC 字段修补

### 起因

ROCE 首次全量回填后 Top 20 严重失真：Accenture 869% / Visa 622% / Apple 230% — 全是"大现金 + 小有形资产"公司。

根因：`backend/pipeline/sec_edgar/company_facts.py` 的 `CORE_CONCEPTS` 缺关键 tag，SEC companyfacts 拉取时 short_term_investments 等字段从未入库。

### 修补

`CORE_CONCEPTS` 增加 7 个 tag：

| 新 tag | 用途 |
|---|---|
| ShortTermInvestments | STI 主源 |
| MarketableSecuritiesCurrent | Apple 类公司用此字段记 STI |
| LongTermDebtCurrent | 一年内到期长债（PRD OperatingCurrentLiab 扣减项） |
| LongTermDebtNoncurrent | 非流动长期借款（净债计算） |
| OperatingLeaseLiabilityCurrent | 一年内经营租赁负债 |
| FinanceLeaseLiabilityCurrent | 一年内融资租赁负债 |
| CommercialPaper | 商业票据（Apple 等公司的"短期借款"） |

### 批量入口

`backend/pipeline/sec_edgar/backfill_tags.py` — 从 DB 取所有美股 cik，逐家调 `ingest_company`。fact_id 哈希按 concept + filed 唯一，新 tag 数据是纯增量。

### 重拉结果

```
total=548, ok=546, errors=2
约 2.3 万条新 fact 入库
耗时约 4 分钟
```

| 新 concept | 行数 |
|---|---:|
| ShortTermInvestments | 3,307 |
| MarketableSecuritiesCurrent | 1,114 |
| LongTermDebtCurrent | 5,192 |
| LongTermDebtNoncurrent | 7,526 |
| OperatingLeaseLiabilityCurrent | 3,170 |
| CommercialPaper | 2,001 |
| FinanceLeaseLiabilityCurrent | 795 |

### ROCE 重跑对比

| 公司 | 修补前 | 修补后 | 状态 |
|---|---:|---:|---|
| Cisco | 213% | 22% | ✓ 真实 ROCE |
| Microsoft | n/a | 66% | ✓ 通过 |
| Medtronic | n/a | 52% | ✓ 通过 |
| Apple | 230% 假阳 | Q5_RECENT_NEG_CAP | ✓ 正确捕获 |
| Visa | 622% 假阳 | Q5_RECENT_NEG_CAP | ✓ 正确捕获 |

### fail_reason 分布

```
Q5_RECENT_NEG_CAP_OR_MISSING: 177  (大现金/负 NWC 公司)
通过 (NULL fail_reason):       153  (25.6%)
Q3_FAIL_MEDIAN:                107  (5Y 中位数 < 20%)
Q1_INSUFFICIENT_HISTORY:        43  (财年不足 5 年)
Q3_FAIL_COUNT:                  17  (5Y 中位数 OK 但 < 4 年 ≥ 20%)
```

剩余少量虚高数值（Fair Isaac 877% / Moody's 563% / Accenture 502%）是轻资产服务业 ROCE 公式的固有特性 — Pulak 书亦承认此类数值不可线性比较但仍正确反映"极高资本效率"，PRD 第 4 节亦未要求排除。

---

## 六、当前数据状态

```sql
SELECT source_type, COUNT(*) FROM fact GROUP BY source_type;
```

| source_type | 行数 | 说明 |
|---|---:|---|
| SEC | 1,074,000+ | 含新增 7 个 tag |
| AKSHARE | 26,629 | A 股旧数据，M2.x fallback 用 |
| TS-FS | 13,874 | A 股新数据，含 accepted_date |

```sql
SELECT metric_name, COUNT(*) FROM metric_periodic GROUP BY metric_name;
```

| metric_name | rows |
|---|---:|
| capital_employed / ebit / net_ppe / nwc_ex_cash / operating_current_assets / operating_current_liabilities / roce | 各 4,489 |
| q3_pass_5y / q4_strong_track_record | 各 497 |
| roce_5y_median / roce_10y_median | 各 497 |

metric_lineage_log: 32,161 行。

---

## 七、关键决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-05-11 | fact.account_code 退化处理 | M1 重构时退化为字面 "us-gaap"，helpers 用 concept 字段查实际 tag，不再迁移底层 1M 行数据 |
| 2026-05-11 | 跨 source 回退（TS-FS → AKSHARE） | 10 家 TS-FS 数据完整、40 家其余 A 股仍依赖 AKSHARE；helpers 自动跨源回退 |
| 2026-05-11 | 严格年报模式（annual_only=True） | Tushare / SEC 数据均混合季报 + 年报，按 fiscal_year_end_month 精确匹配避免季报混入 |
| 2026-05-11 | Q5_RECENT_NEG_CAP 窗口逻辑 | 首版按"最近 5 个 valid 年"取窗口导致 Apple/Visa 假阳，改为"最近 5 个完整财年"含 invalid |
| 2026-05-11 | SEC CORE_CONCEPTS 扩 7 个 tag | OperatingCurrentLiab 扣减项 + STI 扣减项均依赖这些 tag，否则美股 ROCE 系统性失真 |
| 2026-05-11 | 每模块独立 formula_version | metric_periodic 唯一键 (cid, period_end, metric_name, formula_version)，便于各模块单独升级共存 |
| 2026-05-11 | 血缘日志 source_code = ZERO_FALLBACK | 缺失置 0 的字段也记一行 lineage，便于审计追溯哪些字段是缺失 |
| 2026-05-11 | FCF 优先 CFO-CapEx 而非 fcf_reported | Tushare fcf 字段口径未文档化（茅台 2019 = -79B 异常），CFO-CapEx 与 PRD R2 公式一致 |
| 2026-05-11 | interest_coverage 用 abs(interest_expense) | A 股 Tushare finance_expense 可能为负（理财收益压低费用），取绝对值保证语义 |
| 2026-05-11 | EBIT 跨模块共享 ebit_by_year | leverage 计算 net_debt/EBIT 与 interest_coverage 时复用 ROCE 求出的 EBIT，避免重复求解 |
| 2026-05-11 | get_fact_value_asof 三轨可见性 | accepted_date → available_date → period_end + disclosure_lag 兜底；AKSHARE 旧数据双 NULL 时仍可被取到 |
| 2026-05-11 | get_fiscal_year_end 用 NI 取年最大判定 | Revenues 月份众数对 MSFT/AAPL 等均匀分布数据失败；NI 全年 > 单季，每年最大对应财年末 |
| 2026-05-11 | valuation as_of 默认 year_range 末年 12-31 | 截面估值反映"回测点时"语义而非"当前最新"，便于 M7 回测复用 |
| 2026-05-11 | dilution 拆股检测 ratio ≥ 1.5x or ≤ 0.67x | Apple 2020 4:1 拆股触发 POSSIBLE_STOCK_SPLIT_2020，提示 CAGR 信号失真 |

---

## 八、M2.3 杠杆与现金质量

### 交付

| 文件 | 输出 |
|---|---|
| `backend/metrics/leverage.py` | LeverageComponents：net_debt / net_debt_ebit / interest_coverage / current_ratio |
| `backend/metrics/cash_quality.py` | CashQualityComponents：cfo_ni_ratio / fcf；CashQualityGate 截面统计 |

### 关键决策

- **EBIT 跨模块共享**：`compute_leverage_series` 接受 `ebit_by_year` 参数，由 compute.py 调度时把 ROCE 计算结果传入避免重复求 EBIT。
- **interest_coverage 用 abs(interest_expense)**：A 股 Tushare finance_expense 可能为负（理财收益压低费用），取绝对值保证语义。
- **FCF 公式优先级调换**：原"优先 fcf_reported → 回退 CFO-CapEx"改为反向。Tushare `fcf` 字段口径未文档化（茅台 2019 = -79B 异常），CFO - CapEx 与 PRD R2 公式一致更稳定。
- **缺失字段降级标签**：ALL_DEBT_FIELDS_MISSING / STI_ZEROED / NET_DEBT_NEGATIVE / INTEREST_EXPENSE_ZERO_OR_MISSING / EBIT_MISSING_FOR_LEVERAGE_RATIO / CFO_MISSING / NET_INCOME_NONPOSITIVE / FCF_FROM_REPORTED_FALLBACK / FCF_NOT_AVAILABLE

### R1 / R2 风险信号 sanity check（实测）

- R1 高杠杆（net_debt/EBIT > 4）：Simon Property / Prologis（REIT 应 Q0 排除）/ Carrier / Coherent（并购加杠杆）
- R1 利息保障 < 3：Okta/Rivian/HubSpot/Cloudflare/MongoDB（全部负值 — 亏损成长股 EBIT<0）
- R2 CFO/NI 5Y 连续 < 0.7：中国建筑 / Tapestry / Lennox / McKesson（低毛利 / 项目回款慢）
- R2 FCF<0 ≥ 3 年：PG&E / Edison / Exelon（电力公用事业重资产）/ Rivian（烧钱）/ First Solar

---

## 九、M2.4 稀释与估值

### 交付

| 文件 | 输出 |
|---|---|
| `backend/metrics/dilution.py` | DilutionYear（年度 shares_outstanding）+ DilutionGate（share_count_cagr_5y + 拆股检测 POSSIBLE_STOCK_SPLIT_{year}） |
| `backend/metrics/valuation.py` | ValuationSnapshot 截面：market_cap / pe_ttm / ev_ebit / enterprise_value |

### helpers 同步升级

1. **`get_fact_value_asof` 三轨可见性过滤**：
   - 轨 1：accepted_date ≤ asof（SEC/TS-FS）
   - 轨 2：accepted_date NULL + available_date ≤ asof
   - 轨 3：双 NULL 时用 period_end + disclosure_lag_days（默认 120 天）作隐含可见期 — 兼容 AKSHARE 旧数据
2. **`annual_only` 参数**：仅取月份匹配 fiscal_year_end_month 的 fact，避免季报混入截面计算
3. **`get_fiscal_year_end` 算法升级**：原用 Revenues 月份众数（MSFT/AAPL 等季报+年报均匀分布判定失败），改为"NetIncomeLoss 每年最大值对应的 period_end 月份众数"。验证：MSFT 6 月 ✓ / AAPL 9 月 ✓ / NVDA 10 月 ✓ / Walmart 1 月 ✓

### Valuation 数值合理性抽样

| 公司 | mc | PE_TTM | 评价 |
|---|---:|---:|---|
| MSFT | 3103B | 35.2x | ✓ 实际 ~35x |
| Apple | 3765B | 40.2x | ✓ 实际 ~38-40x |
| Nvidia | n/a | n/a | 见下 |
| 美的集团 | 616B | 13.8x | ✓ 实际 ~13x |
| 茅台 | **11134B** | **143x** | ❌ A 股 close 复权价 V1 遗留 bug（实际 mc ~1.7T 元，PE ~21x） |
| JPM | 55B | 1.1x | ❌ 金融股 shares 量级问题（Q0 本就排除） |

### Dilution 抽样

| 公司 | 5Y CAGR | 备注 |
|---|---:|---|
| MSFT | -0.66% | 持续回购 ✓ |
| 茅台 | 0.00% | 股本稳态 ✓ |
| 京东方 | +1.59% | 历年小幅增发 |
| Apple | +26.74% | 标 POSSIBLE_STOCK_SPLIT_2020（2020 4:1 拆股噪声） |
| 真实高稀释 | INSMED 13.87% / EQT 11.25% / KKR 9.66% | 排除拆股后真实稀释信号 ✓ |

---

## 十、M2.5 调度器整合 + 最终全量回填

### compute.py 重构

`persist_all_metrics_for_company` 顶层入口，调度 5 个子模块 + 一次性 `_build_fact_meta_lookup`：

```
roce_series   ← compute_roce_series                     →  ebit_by_year
lev_series    ← compute_leverage_series(ebit_by_year)
cq_series     ← compute_cash_quality_series
dil_series    ← compute_dilution_series
val_snap      ← compute_valuation_snapshot(asof=date(year_range[1], 12, 31))

(all_fact_ids → fact_meta_lookup, 一次查 SEC/TS-FS/AKSHARE 元数据)

_persist_roce / _persist_leverage / _persist_cash_quality / _persist_dilution / _persist_valuation
```

`_upsert_metric` 与 `_write_lineage_rows` 参数化 `formula_version`，5 个子模块各自版本号：roce_v1 / leverage_v1 / cash_quality_v1 / dilution_v1 / valuation_v1。

### 最终全量回填结果

```
total=598, ok=598, errors=0
year_rows=65,887（roce 7×11 + leverage 4×11 + cash_quality 2×11 + dilution 1×11）
section_rows=6,793（roce 4 + cash_quality 4 + dilution 1 + valuation 4 = 13 per company × ~520 有效公司）
lineage_rows=86,811
pass_5y_count=172 (28.8%)
strong_count=146 (24.4%)
耗时 12 分钟
```

### 指标分布按 formula_version

| formula_version | distinct metrics | total rows |
|---|---:|---:|
| roce_v1 | 11 | 33,699 |
| leverage_v1 | 4 | 20,144 |
| cash_quality_v1 | 6 | 12,530 |
| dilution_v1 | 2 | 3,979 |
| valuation_v1 | 4 | 2,328 |

### V2 strict 模式（PE ≤ 14.9 + Q3 通过）候选抽样

| 公司 | PE | ROCE 5Y |
|---|---:|---:|
| O'Reilly Auto | 2.0x | 103.5% |
| 紫金矿业 | 2.5x | 47.6% |
| EOG Resources | 4.2x | 23.9% |
| Tractor Supply | 5.2x | 47.7% |
| Aptiv | 5.7x | 27.3% |
| General Motors | 6.2x | 35.6% |
| Deckers Outdoor | 8.2x | 89.9% |
| Charter Communications | 8.4x | 37.5% |
| Halliburton | 9.2x | 34.6% |
| Dollar General | 10.0x | 48.3% |

> 部分 PE 异常低（< 3x）来自金融股或 shares 量级 bug，M3 Q0 层会排除。

---

## 十一、已知数据缺陷与后续修补项

| 项 | 现象 | 修补方案 |
|---|---|---|
| A 股 market_bar 复权价 | 茅台 2024-12-31 close=8863 元（实际 ~1500），mc/PE 严重失真 | 重拉 A 股 market_bar 用不复权价；或接 Tushare daily_basic 直接取 total_mv / pe_ttm |
| 金融股 shares_outstanding 量级 | JPM mc=55B vs 实际 600B+；shares 取了 WeightedAverage 而非真实 outstanding | 字段映射调整，或排除金融股（Q0 已覆盖） |
| TSLA fy_end_month 推断 = 3 | 实际 12 月底；helpers 启发式对 Tesla 失效（早年财年异常） | 用 SEC submissions fiscalYearEnd 字段权威判定 |
| valuation `pe=0` 异常 | mc=0 时未跳过除法 | 已修（mc > 0 才算 PE），影响 ~ 5-10 家公司，下次回填后消失 |

---

## 十二、未完成（路线图剩余）

| # | 任务 | 状态 |
|---|---|---|
| M3 | 三层漏斗筛选引擎（Q/R/V Layer） | 待 |
| M4 | AI 风险层（ChatGPT + MiniMax + Fernet 加密） | 待 |
| M5 | API 层重写 + 用户 API Key 绑定 | 待 |
| M6 | 前端 6 页面重构 | 待 |
| M7 | 月度再平衡点时回测 + 端到端 smoke | 待 |
| M1.9 | SEC filing 文本与元数据（可与 M3 并行） | 待 |
| M1.10 | Polygon News 接入（可与 M3 并行） | 待 |
| 数据修补 | A 股 close 不复权 / 金融股 shares / TSLA 财年 | 后续 |

---

## 九、复现命令

```bash
# 启动后端（仅基础路由，V2 路由待 M5）
cd /srv/workspaces/zheyang/darwen
/opt/miniconda3/envs/darwen/bin/uvicorn backend.main:app --host 0.0.0.0 --port 15001 --reload

# ROCE 单股快速验证
/opt/miniconda3/envs/darwen/bin/python -c "
from backend.database import SessionLocal
from backend.metrics.roce import compute_roce_series, evaluate_quality_gate
db = SessionLocal()
series = compute_roce_series(db, 'US_0000789019', (2014, 2024))
gate = evaluate_quality_gate(series)
print(f'MSFT 5Y={gate.median_5y*100:.1f}% pass={gate.pass_5y_gate} strong={gate.strong_track_record}')
"

# 全量回填（带 truncate）
mysql -h 127.0.0.1 -uroot -pdarwen_dev_123 darwen -e "TRUNCATE TABLE metric_periodic; TRUNCATE TABLE metric_lineage_log;"
/opt/miniconda3/envs/darwen/bin/python -m backend.metrics.compute

# SEC 字段增量回填（CORE_CONCEPTS 变更后）
/opt/miniconda3/envs/darwen/bin/python -m backend.pipeline.sec_edgar.backfill_tags

# fail_reason 分布巡检
mysql -h 127.0.0.1 -uroot -pdarwen_dev_123 darwen -e "
SELECT notes, COUNT(*) FROM metric_periodic WHERE metric_name='q3_pass_5y' GROUP BY notes ORDER BY 2 DESC;
"

# Top 20 通过门槛公司
mysql -h 127.0.0.1 -uroot -pdarwen_dev_123 darwen -e "
SELECT c.market, c.name, ROUND(m5.value*100,1) AS roce_5y_pct
FROM metric_periodic m5 JOIN company c ON m5.company_id=c.company_id
WHERE m5.metric_name='roce_5y_median' AND m5.value IS NOT NULL
  AND EXISTS (SELECT 1 FROM metric_periodic q3 WHERE q3.company_id=m5.company_id AND q3.metric_name='q3_pass_5y' AND q3.value=1)
ORDER BY m5.value DESC LIMIT 20;
"
```

---

*记录人：Claude Agent | 日期：2026-05-11 | 阶段：M2 全部完成（M2.1-M2.5 + SEC 字段修补） — 已落库 5 类指标 + 18 个 distinct metric_name 覆盖 ROCE / 杠杆 / 现金质量 / 稀释 / 估值*
