# 工作记录 2026-06-21

## 主题：「我的股票池」改造 —— 取消分组 + 实时股价/市盈率 + 设为首页

承接 2026-06-20。用户对「我的股票池」提三点需求并经 plan mode 确认实现方式：
① 取消分组、平铺显示选中股票；② 展示最新收盘价 + 市盈率（PE 用真 TTM 4 季净利润）；
③ 作为前端首页。所有改动增量、可回滚，前后端均验证。

---

## 一、PE 真 TTM（YTD 差分法）

**背景**：`valuation.py` 原 `pe_ttm` 用「最近年报净利润」作 TTM 代理（非真 TTM）。用户要求 PE 分母
用 TTM 滚动 4 季。核查数据发现 fact 表 net_income 是 **YTD 累计值**（AMD 2025 各季 0.709→1.581→
2.824→4.335 递增），故不能简单 4 季求和。

**实现**：
- `backend/metrics/helpers.py` 新增 `get_fact_series_asof`：取 ≤asof 点时可见的逐期 fact 序列
  （含季报，按 period_end 降序、每期一条），复用三轨可见性。
- `backend/metrics/valuation.py` 新增 `_net_income_ttm`（YTD 差分）：
  - 最新一期即年报（period_end 月==财年末月）→ 直接用其全年值；
  - 否则季报 YTD → `TTM = 最近完整财年 + 本年最新YTD − 去年同期YTD`；
  - 关键项缺失 → 降级回最近年报，标 `PE_TTM_ANNUAL_PROXY`。
- `compute_valuation_snapshot` 第 4 步改用 `_net_income_ttm`。

**验证**：AMD（截至 2026-06-21）net_income_ttm = **5.009B**（=4.335+1.383−0.709，真 TTM）而非年报
4.335；pe_ttm = 65.75（市值 329.4B）。

---

## 二、后端平铺接口 `/v2/watchlists/my-stocks`

- `backend/schemas/v2.py` 新增 `MyStockOut`（company/ticker/name/market/industry/note/source_run_id/
  added_at + latest_close/latest_trade_date/pe_ttm/as_of）。
- `backend/api/watchlist.py`：
  - `GET /v2/watchlists/my-stocks`：合并当前用户所有池的 item（按 company_id 去重），每条复用
    `compute_valuation_snapshot(today)` 带最新收盘价 + PE(TTM)。注册在 `/watchlists/{wid}` 之前避免路由冲突。
  - `DELETE /v2/watchlists/my-stocks/{company_id}`：跨池移除（当前用户所有池一并删）。
- 后端模型保留多池结构不动（向后兼容），「取消分组」在视图层合并。

**验证（HTTP 端到端）**：add-company AMD → my-stocks 返回 `AMD close=201.99 pe_ttm=65.75 as_of=2026-06-21`
→ 跨池移除 → 剩 0。

---

## 三、前端

- `MyWatchlist.vue` **重写**为单一平铺表格：Ticker | 公司 | 市场 | 最新价 | 市盈率(TTM, 带 ⓘ 口径说明) |
  行业 | 操作（详情/移除）+ 刷新行情按钮。移除既有左右双栏/池管理。调 `getMyStocks`/`removeMyStock`。
- `api/index.js` 加 `getMyStocks` / `removeMyStock`。
- `CompanyDetailV2.vue` 入池**简化为一键加默认池**「我的股票池」（删 select 选池面板 + 相关 ref/孤儿 CSS）。
- **首页**：`router/index.js` `/` redirect 改 `/my-watchlists`；`App.vue` 导航「我的股票池」置首位。

**验证**：`npm run build` 通过（多次）；vite(15002) HMR。

---

## 四、数据新鲜度备注

- `market_bar` 整体最新 2026-06-18；个股参差（如 AMD 仅到 2026-03-27）。「最新收盘价」取 market_bar
  ≤today 最新一条，属数据管道覆盖范围，非本次逻辑问题。后续如需更勤的行情可定时拉取。

---

## 五、Git / 文档

- 代码：valuation/helpers（PE TTM）、watchlist API/schema（my-stocks）、前端 MyWatchlist/CompanyDetailV2/
  router/App/api。
- 文档：PRD V1' 补 PE_TTM 真 TTM 口径；本日志；progress。
- 推送至 GitHub `dolphinfafa/darwen` main。

---

## 六、待办 / 留待决策

- 行情新鲜度：如需「更接近实时」，可加定时拉取最新日线（yfinance/Tushare）或盘中行情 API（成本权衡）。
- my-stocks 为逐股算估值（N+1），个人池量小可接受；如池很大可批量优化。
- A 股 net_income 季报同为 YTD，差分法通用；缺季报则降级年报。
